import { LoadchildDirective } from './loadchild.directive';

describe('LoadchildDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadchildDirective();
    expect(directive).toBeTruthy();
  });
});
